# mysite

## GitHub Pages configuration

This project is configured for GitHub Pages project-site deployment with:

- **Base path:** `/mysite/`

The base path is inferred from the repository name (`mysite`), so built assets resolve correctly when hosted at:

- `https://<owner>.github.io/mysite/`
